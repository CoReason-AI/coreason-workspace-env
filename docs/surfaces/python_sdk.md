# Python SDK

The Python SDK allows programmatic embedding of the CoReason platform into other Python applications.

*   **Location:** `src/sdk/`
*   **Usage:** `import coreason`

The SDK yields asynchronous iterators for long-running streams, ensuring that integrating applications can react to agent state changes in real-time.
